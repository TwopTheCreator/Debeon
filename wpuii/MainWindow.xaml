<ui:FluentWindow x:Class="wpuii.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="WPUII - WPF-UI Showcase"
        Height="800"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="WPUII Application"
                     Icon="pack://application:,,,/wpuii;component/Assets/app.ico"
                     ShowMaximize="True"
                     ShowMinimize="True"/>

        <ui:NavigationView Grid.Row="1"
                          x:Name="NavigationView"
                          IsBackButtonVisible="Visible"
                          IsPaneToggleVisible="True"
                          OpenPaneLength="250"
                          PaneDisplayMode="Left">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="Home"
                                      Icon="{ui:SymbolIcon Home24}"
                                      Tag="home"/>
                <ui:NavigationViewItem Content="Dashboard"
                                      Icon="{ui:SymbolIcon DataUsage24}"
                                      Tag="dashboard"/>
                <ui:NavigationViewItem Content="Controls"
                                      Icon="{ui:SymbolIcon GridDots24}"
                                      Tag="controls"/>
                <ui:NavigationViewItem Content="Data Grid"
                                      Icon="{ui:SymbolIcon Table24}"
                                      Tag="datagrid"/>
                <ui:NavigationViewItem Content="Charts"
                                      Icon="{ui:SymbolIcon ChartMultiple24}"
                                      Tag="charts"/>
                <ui:NavigationViewItem Content="Forms"
                                      Icon="{ui:SymbolIcon FormNew24}"
                                      Tag="forms"/>
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="Settings"
                                      Icon="{ui:SymbolIcon Settings24}"
                                      Tag="settings"/>
                <ui:NavigationViewItem Content="About"
                                      Icon="{ui:SymbolIcon Info24}"
                                      Tag="about"/>
            </ui:NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame"
                   NavigationUIVisibility="Hidden"
                   Margin="0"/>

        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
